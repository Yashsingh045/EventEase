---
import { testimonials } from "../../dummyData.js";
---

<section class="py-16 md:py-24 bg-gradient-to-b from-gray-150 to-gray-100">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <h2
                class="font-serif text-4xl md:text-5xl font-bold mb-4 bg-gradient-to-r from-blue-500 to-blue-800 bg-clip-text text-transparent"
            >
                What Our Users Say
            </h2>
            <p class="text-lg md:text-xl text-gray-600 max-w-2xl mx-auto">
                Trusted by thousands of event organizers worldwide
            </p>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {
                testimonials.map((testimonial) => (
                    <div class="card bg-white border-2 border-black shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] hover:shadow-[8px_8px_0px_0px_rgba(0,0,0,1)] transition-all duration-300 hover:-translate-y-1 rounded-none">
                        <div class="card-body">
                            <div class="flex items-center gap-4 mb-4">
                                <div class="flex-1">
                                    <h3 class="text-xl font-semibold text-gray-900">
                                        {testimonial.name}
                                    </h3>
                                    <p class="text-sm text-gray-500">
                                        {testimonial.role}
                                    </p>
                                </div>
                            </div>
                            <div class="flex gap-1 mb-4">
                                {Array.from({ length: testimonial.rating }).map(
                                    () => (
                                        <span class="text-yellow-400 text-xl">
                                            ★
                                        </span>
                                    ),
                                )}
                            </div>
                            <p class="text-gray-700 leading-relaxed italic">
                                "{testimonial.text}"
                            </p>
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>
